{
  "name": "dpsk-harness-analysis",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node scripts/build-site.mjs && node scripts/build-markdown.mjs",
    "check": "node --test tests/markdown-renderer.test.mjs && node scripts/check-site.mjs && node scripts/check-ch35.mjs --analysis-root . --output-root . && node scripts/build-markdown.mjs --check",
    "check:index": "node scripts/generate-ch35.mjs --check --analysis-root . --upstream-root ../deepseek-harness --output-root ."
  },
  "engines": {
    "node": ">=22"
  }
}
